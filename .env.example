# No secrets required for the current ad setup (script src + zone IDs live
# in src/config/adProviders.config.ts). Add VITE_-prefixed vars here if a
# provider you add later needs an API key at build time.
